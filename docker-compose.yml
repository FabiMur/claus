services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: claus-qdrant
    ports:
      - "6333:6333" # HTTP / web UI
      - "6334:6334" # gRPC (used by claus)
    volumes:
      - ./qdrant_storage:/qdrant/storage
